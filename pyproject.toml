[project]
name = "iss-group-24"
version = "0.1.0"
description = "Few-shot object localization — dataset cleaner and training pipeline."
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0",
    "Pillow>=10.0",
    "torch>=2.4",
    "torchvision>=0.19",
    "litert-torch>=0.9.0",
    "matplotlib>=3.7",
]

Notebook:
    - Mount shared folder on google colab
    - Don't copy or download to local runtime, use entirely files from mounted drive
    - Save the files on mounted shared drive



Model FineTuning plan:
  Implement if you agree, talk to me if not:

  Phase 1 (VizWiz base set stages): stage 1.1 = warm up frozen backbone, stage 1.2 = partial unfreeze.
  Phase 2 (VizWiz support+query set, HOTS support&query + InsDet support&query, HOPE negatives): stage 2.1 = warm up frozen backbone, stage 2.2 = partial
  unfreeze, stage 2.3 = major/full unfreeze.

  You can farm 4 support images from VizWiz through cropping based on the bbox and rotating, using the original image as scene/query.

  Adjust hyperparams for all phases and stages they way you think will maximize mAP performance.
  Ensure architecture such as loss function will be good to maximize mAP.

  Phase 1 should use negatives and val only from the VizWiz base set dataset.
  Phase 2 should use the VizWiz support&query (positive & negative), HOTS+InsDet dataset (positives only), negatives from all of HOPE dataset.

